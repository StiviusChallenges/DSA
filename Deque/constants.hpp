#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

enum class ExpandingPolicy {
    Front,
    Back
};

const int EXPANDING_BLOCKS = 2;
const int BLOCK_SIZE = 8;
const int INVALID_INDEX = -1;

#endif // CONSTANTS_HPP
